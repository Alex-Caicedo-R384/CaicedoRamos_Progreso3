<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="CaicedoRamos_Progreso3.Views.ResultsPage"
             Title="ResultsPage"
             BackgroundColor="#5e5e5e">
    <ListView x:Name="ListaArtistas">
        <ListView.ItemTemplate>
            <DataTemplate>
                <ViewCell>
                    <StackLayout>
                        <Label Text="{Binding NombreArtista}" FontAttributes="Bold" FontSize="Large" />
                        <Label Text="{Binding ID}" />
                        <Label Text="{Binding Pais}" />
                        <Label Text="{Binding FechadeActualización}" />
                        <Label Text="{Binding FechaNacimiento}" />
                        <Label Text="{Binding FechaFallecimiento}" />
                        <BoxView HeightRequest="1" Color="#61071d" />
                    </StackLayout>
                </ViewCell>
            </DataTemplate>
        </ListView.ItemTemplate>
    </ListView>
</ContentPage>